# ── Database ──────────────────────────────────────────────────────────────────
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=VionaLushta334)
DB_NAME=online_book_store

# ── JWT ───────────────────────────────────────────────────────────────────────
JWT_SECRET=bookstore_jwt_secret_key_2024

# ── Server ────────────────────────────────────────────────────────────────────
PORT=5000

# ── Frontend URL (used inside verification and reset email links) ─────────────
FRONTEND_URL=http://localhost:5173

# ── SMTP ─────────────────────────────────────────────────────────────────────
# Fill in a REAL email account below. The sender can be any Outlook/Hotmail/Gmail
# account you own. Emails will be delivered to @ubt-uni.net addresses.
#
# IF YOUR ACCOUNT HAS MFA / TWO-STEP VERIFICATION:
#   Outlook → https://account.microsoft.com/security
#             → Advanced security options → App passwords → Create one
#             → Use that generated password as SMTP_PASS
#   Gmail   → https://myaccount.google.com/security
#             → 2-Step Verification → App passwords → Generate
#
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=your-email@outlook.com
SMTP_PASS=your-password
